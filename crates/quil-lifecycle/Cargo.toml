[package]
name = "quil-lifecycle"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["macros", "rt", "signal", "sync", "time"] }
tokio-util = "0.7"
tracing = "0.1"
